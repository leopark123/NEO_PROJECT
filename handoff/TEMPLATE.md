# 📦 [模块名] API 交接文档

> **版本**: v1.0  
> **负责方**: [Codex / Claude]  
> **创建日期**: YYYY-MM-DD  
> **关联任务**: TASK-Sn-xx

---

## 1. 概述

[简要描述模块功能，1-2句话]

---

## 2. 公开接口

```csharp
namespace NEO.Core
{
    public interface IXxxService
    {
        // 接口定义
    }
    
    public class XxxService : IXxxService
    {
        // 实现签名（不含实现细节）
    }
}
```

---

## 3. 数据模型

```csharp
public readonly struct XxxData
{
    public long TimestampUs { get; }
    // 其他字段
}
```

---

## 4. 线程模型 🔴 **[必填]**

| 角色 | 线程 | 说明 |
|------|------|------|
| 写入者 | [DSP线程/采集线程/...] | [描述] |
| 读取者 | [渲染线程/UI线程/...] | [描述] |

**线程安全性**: [线程安全 / 非线程安全 / 读写分离]

**锁策略**: [无锁 / 使用lock / 使用ReaderWriterLock / ...]

---

## 5. 时间戳语义 🔴 **[必填]**

| 输入/输出 | 时间戳含义 | 单位 |
|-----------|-----------|------|
| 输入 | [样本中心时间 / 包起点 / ...] | μs |
| 输出 | [样本中心时间 / 窗口中心 / ...] | μs |

**时钟域**: [Device / Host / Unknown]

**精度**: [估计精度，如 ±10ms]

---

## 6. 数据契约 🔴 **[必填]**（接口任务必须填写）

| 属性 | 值 | 说明 |
|------|-----|------|
| 采样率 | [如 160 Hz] | |
| 通道数 | [如 4] | |
| 数据类型 | [如 double] | |
| 时间戳单位 | μs | 必须是微秒 |
| Gap 表达方式 | [如 QualityFlags.Gap] | |
| 饱和表达方式 | [如 QualityFlags.Clipped] | |

---

## 7. 使用示例

```csharp
// 初始化
var service = new XxxService(config);

// 基本使用
var result = service.DoSomething(input);

// 清理
service.Dispose();
```

---

## 8. 性能特征

| 指标 | 值 | 测试条件 |
|------|-----|----------|
| 吞吐量 | xxx/sec | [条件] |
| 延迟 P50 | xxx μs | [条件] |
| 延迟 P99 | xxx μs | [条件] |
| 内存占用 | xxx MB | [条件] |

---

## 9. 已知限制

1. [限制1: 描述]
2. [限制2: 描述]

---

## 10. 依赖项

| 依赖模块 | 接口/类 | 用途 |
|----------|---------|------|
| [模块A] | IXxx | [用途] |
| [模块B] | YyyService | [用途] |

---

## 11. 测试覆盖

| 测试类 | 测试项 | 状态 |
|--------|--------|------|
| XxxTests | 正常路径 | ✅ |
| XxxTests | 边界条件 | ✅ |
| XxxTests | 异常处理 | ✅ |
| XxxTests | 并发安全 | ✅ |

---

## 12. 变更历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | YYYY-MM-DD | 初始版本 |

---

**文档结束**
