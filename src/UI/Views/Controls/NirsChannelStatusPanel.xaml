<UserControl x:Class="Neo.UI.Views.Controls.NirsChannelStatusPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="NIRS 通道状态"
                       Foreground="{DynamicResource TextOnDarkBrush}"
                       FontSize="{StaticResource FontSize.Caption}"
                       FontWeight="SemiBold"/>
            <TextBlock Text="rSO2: XX% / --% / Fault / Blocked"
                       Foreground="{DynamicResource TextDarkTertiaryBrush}"
                       FontSize="{StaticResource FontSize.Caption}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      Margin="0,8,0,0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Channels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2,0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0"
                                      Style="{StaticResource ToggleSwitchStyle}"
                                      IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                      VerticalAlignment="Center"/>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding ChannelLabel}"
                                       Foreground="{DynamicResource TextOnDarkBrush}"
                                       FontSize="{StaticResource FontSize.Caption}"
                                       Margin="6,0,6,0"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
