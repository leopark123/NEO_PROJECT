<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================================================================ -->
    <!-- Sprint 1.3: TextBox styles                                       -->
    <!-- Source: NEO_UI_Development_Plan_WPF.md §5.3                      -->
    <!-- Height 44px, CornerRadius 4px, Focus border #D81B60              -->
    <!-- ================================================================ -->

    <Style x:Key="NeoTextBoxStyle" TargetType="TextBox">
        <Setter Property="FontFamily"    Value="{StaticResource Font.UI}"/>
        <Setter Property="FontSize"      Value="{StaticResource FontSize.Body}"/>
        <Setter Property="MinHeight"     Value="{StaticResource Dim.ControlHeight}"/>
        <Setter Property="Padding"       Value="{StaticResource Dim.InputPadding}"/>
        <Setter Property="Background"    Value="{StaticResource InputBackgroundBrush}"/>
        <Setter Property="Foreground"    Value="{DynamicResource TextOnSurfaceBrush}"/>
        <Setter Property="CaretBrush"    Value="{DynamicResource TextOnSurfaceBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource SurfaceDarkBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Dim.CornerRadiusAll}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="0"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush"
                                    Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PasswordBox style (same visual as TextBox) -->
    <Style x:Key="NeoPasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="FontFamily"    Value="{StaticResource Font.UI}"/>
        <Setter Property="FontSize"      Value="{StaticResource FontSize.Body}"/>
        <Setter Property="MinHeight"     Value="{StaticResource Dim.ControlHeight}"/>
        <Setter Property="Padding"       Value="{StaticResource Dim.InputPadding}"/>
        <Setter Property="Background"    Value="{StaticResource InputBackgroundBrush}"/>
        <Setter Property="Foreground"    Value="{DynamicResource TextOnSurfaceBrush}"/>
        <Setter Property="CaretBrush"    Value="{DynamicResource TextOnSurfaceBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource SurfaceDarkBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Dim.CornerRadiusAll}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="0"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush"
                                    Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

